# Accounta 要求仕様書

この文書は、Accountaで利用者が何をしたいか、各バージョンで何ができるようになるかを定義する。

技術的な実現方法や細かな画面仕様は、バージョン別の実装指示書、コードおよびテストで管理する。

## Accountaについて

Accountaは、個人事業主、マイクロ法人、小規模組織および会計学習者を対象とする、シンプルな会計・予実管理アプリである。

利用者が予算・実績や固定資産を管理し、仕訳から総勘定元帳、損益計算書および貸借対照表を作成できることを目指す。また、登録したデータをExcelで利用できるようにする。

一般ユーザーは自分が所属する組織のデータを利用し、会社管理者は所属組織のマスタを管理する。システム管理者は、それらに加えて会社情報を管理する。会計学習者は、実際のデータ構造を使ってSQLやExcelを学び、仕訳から帳簿や財務諸表が作られる流れを確認できる。

## バージョン全体像

- v0.1：予算・実績管理と基本マスタを提供する
- v0.2：簡易的な固定資産管理台帳を追加する
- v0.3：簡易的な仕訳帳と証憑の紐付けを追加する
- v0.4：ローカルMCPサーバーを追加し、仕訳帳を参照できるようにする。以降のバージョンで参照対象を広げる
- v0.5：総勘定元帳と、その集計・詳細出力を追加する
- v0.6：仕訳から簡易的なP/LとB/Sを作成できるようにする
- v0.7～v0.9：各機能の連携、操作性および品質を改善する
- v0.10：証憑のOCR、内容解析および重複判定を追加する
- v0.11：証憑の解析結果から仕訳候補と自動仕訳を生成できるようにする
- v0.12：管理アカウント向けの簡易CMSを追加する
- v0.13：税理士試験・財務諸表論の理論学習スペースを追加する
- v1.0：簡易的なAccountingサービスとして正式公開する
- v1.1：Apache Supersetを使ったダッシュボード表示を追加する
- v1.2以降：厳密な簿記手続、自動計算および自動仕訳等を追加する
- v1.0以降：モバイル端末に適したUIおよびUXを強化する
- 将来構想：SAP S/4HANAと連携し、全社ERPの会計・購買・固定資産・原価等のデータをAccountaで利用できるようにする
- 将来構想：v0.4で導入したMCPを拡張し、AccountaおよびSAPのデータ・業務機能をChatGPTから自然言語で利用できるようにする
- 将来構想：Accountaの管理データ、S/4HANAの業務データおよび分析ノウハウを組み合わせ、AIによる差異分析・原因分析・意思決定支援を実現する

## v0.1

v0.1では、予算・実績管理と、そのための基本情報管理を提供する。

実装・検証時の制御基準は、[Accounta v0.1 実装指示書](docs/design/v0.1_基本機能.md)に定める。

**認証とプロフィール**

- ユーザーIDとパスワードで登録・ログインできる
- メールアドレスなしで利用できる
- 表示名とパスワードを変更できる
- 現在の会社、組織およびユーザー種別を確認できる
- ログアウトし、ログイン中のセッションを管理できる

ユーザーIDは登録後に変更できない。メール認証、パスワードリセット、二要素認証、Passkeys、API Tokenおよびアカウント削除は提供しない。

**会社・組織・権限**

- 利用者は会社と組織に所属する
- 会社は複数の組織を持つことができる
- 業務データは組織ごとに分離する
- 一般ユーザーは他の組織のデータを利用できない
- システム管理者と会社管理者は部門マスタと予実管理科目マスタを管理できる
- 一般ユーザーは部門マスタと予実管理科目マスタを参照できるが、変更できない
- システム管理者は会社情報を登録・編集・削除できる
- 会社管理者と一般ユーザーは会社情報を管理できない

会社では、企業コード、企業名および会計年度開始月を管理する。v0.1では、画面から所属会社や所属組織を切り替える機能は提供しない。

**部門マスタ**

- すべての利用者が部門の一覧を確認できる
- システム管理者と会社管理者は部門を登録できる
- システム管理者と会社管理者はコードと名称を編集できる
- システム管理者と会社管理者は利用状態を有効または無効にできる

利用済みの部門は削除せず、無効化して履歴を維持する。

**予実管理科目マスタ**

- すべての利用者が予実管理科目の一覧を確認できる
- システム管理者と会社管理者は予実管理科目を登録できる
- システム管理者と会社管理者はコード、名称および科目区分を編集できる
- システム管理者と会社管理者は利用状態を有効または無効にできる

利用済みの予実管理科目は削除せず、無効化して履歴を維持する。

**予算・実績**

- 対象年月、部門、予実管理科目、予算・実績区分、金額およびメモを登録できる
- 登録した明細を一覧で確認できる
- 明細を編集・削除できる
- 予算と実績をそれぞれ独立した明細として登録できる
- 同じ条件に対して複数の明細を登録できる
- 予算に存在しない科目の実績も登録できる

金額には0、正数および負数を使用できる。v0.1の実績は手入力データとする。

**ダッシュボード**

- 所属会社と所属組織を確認できる
- 予算明細数と実績明細数を確認できる
- 利用中の部門数と予実管理科目数を確認できる
- 各機能へ移動できる

**Excelエクスポート**

所属組織の次の情報を、1つのExcelファイルとしてダウンロードできる。

- 予算・実績明細 `予算・実績`
- 部門マスタ `部門マスタ`
- 予実管理科目マスタ `予実管理科目マスタ`
- 会社・組織情報 `会社・組織情報`（システム管理者のみ）

各シートの列幅は、Excelで内容を読みやすい幅に調整する。会社管理者と一般ユーザーのExcelには、会社・組織情報を出力しない。

ExcelからAccountaへデータを取り込む機能は提供しない。

**初期データ**

開発・初期利用のため、初期会社、初期組織、システム管理者、会社管理者、一般ユーザー、部門マスタおよび予実管理科目マスタを再現可能な方法で準備できる。初期パスワードはソースコードへ固定値として保存しない。

## v0.2

v0.2では、v0.1に加えて簡易的な固定資産管理台帳を提供する。

実装・検証時の制御基準は、[Accounta v0.2 固定資産管理台帳 詳細設計書](docs/design/v0.2_固定資産管理台帳.md)に定める。

- 固定資産を登録・確認・編集・削除できる
- 資産コード、資産名、選択式の資産区分、その他区分名、取得日、供用開始日、取得価額、耐用年数、部門および備考を管理できる
- 償却方法と残存価額を管理できる
- 当期減価償却費、減価償却累計額および帳簿価額を管理できる
- 売却、除却等の状態を管理できる
- 自分の組織の固定資産だけを利用できる
- ダッシュボードで保有中の固定資産数、取得価額合計および帳簿価額合計を確認できる

資産区分は、建物、建物附属設備、構築物、機械及び装置、船舶、航空機、車両及び運搬具、工具、器具及び備品、ソフトウェア、その他の無形固定資産、リース資産、生物、土地、建設仮勘定およびその他から選択する。「その他」を選択した場合はその他区分名を入力する。資産区分と予実管理科目は別概念として扱い、v0.2では関連付けない。

償却方法は、次から選択できる。

- 定額法
- 定率法（旧）
- 200%定率法（新定率法）
- 生産高比例法
- 級数法
- 償却対象外
- その他

残存価額は任意に設定でき、初期値は0とする。必要に応じて備忘価額として1を設定できる。

v0.2では減価償却費を自動計算しない。利用者が計算した当期減価償却費および減価償却累計額を登録し、帳簿価額を確認するための簡易台帳とする。

旧定率法の償却率、200%定率法の改定償却率や保証率、生産高比例法の総利用可能量等は専用項目にせず、備考へ記録する。詳細な償却計算への対応はv1.0以降とする。

通常のExcelエクスポートへ、固定資産管理台帳 `FixedAssets` を追加する。

## v0.3

v0.3では、v0.2に加えて簡易的な複式簿記による仕訳帳を提供する。

実装・検証時の制御基準は、[Accounta v0.3 仕訳帳・証憑管理 詳細設計書](docs/design/v0.3_仕訳帳と証憑管理.md)に定める。

- 仕訳を登録・確認・編集・削除できる
- 仕訳用勘定科目マスタを予実管理科目マスタとは独立して管理できる
- 借方と貸方の仕訳用勘定科目を選択し、金額を手動で入力できる
- 仕訳日、部門、摘要および備考を管理できる
- 複数の借方・貸方明細を持つ仕訳を登録できる
- 借方合計と貸方合計が一致した仕訳を保存できる
- 自分の組織の仕訳だけを利用できる

仕訳用勘定科目と予実管理科目は別概念として扱う。両者のマッピングと、仕訳実績を予実管理へ反映する方法は後続バージョンで設計する。

v0.3では仕訳を自動生成しない。固定資産の減価償却費を計上する場合も、利用者が固定資産管理台帳を確認して仕訳を手動で登録する。

期首残高が必要な場合は、開始残高に相当する仕訳を手動で登録する。専用の開始手続や自動繰越はv1.0以降とする。

v0.1の手入力実績と仕訳から集計できる会計実績は、別のデータとして扱う。

レシート、領収書、請求書等の証憑ファイルを仕訳へ任意に紐付け、確認および紐付け解除ができる。OCR、自動仕訳および証憑内容の自動判定はv1.0以降とする。

通常のExcelエクスポートへ、仕訳帳 `JournalEntries` を追加する。

## v0.4

v0.4では、ローカル環境で動作するAccounta MCP Serverを追加し、Codexから仕訳帳を参照できるようにする。初めてMCPを導入するための最小構成とし、外部から利用できる業務機能は仕訳帳の参照だけに限定する。マスタ、予算・実績、固定資産、財務諸表などの参照は後続バージョンで順に追加する。

実装・検証時の制御基準は、[Accounta v0.4 MCPサーバー 詳細設計書](docs/design/v0.4_MCPサーバー.md)に定める。

- 同じPCで `php artisan serve` により起動した `http://127.0.0.1:8000/mcp/accounta` へ、CodexがStreamable HTTPで接続できる
- 利用者は、AtlassianのMCPと同じく、CodexからOAuth 2.1で自分のAccountaアカウントにログインして接続する
- ログインした利用者の所属組織の仕訳帳を参照できる
- 仕訳の明細と、紐付く証憑のメタデータを確認できる

MCP ServerはAccountaのモデルおよび業務ロジックを経由してデータを取得し、データベースへの任意SQL実行機能を公開しない。対象組織はログインした利用者から決まり、接続設定やTool引数では切り替えない。

v0.4では参照専用のMCP Toolを1つだけ提供する。予算・実績、固定資産、仕訳、証憑およびマスタの登録・更新・削除は行わない。証憑はファイル内容を無条件に公開せず、ファイル名、種別、登録日時および紐付く仕訳等のメタデータを対象とする。ローカルプロセス接続は提供しない。

接続先の基本は、同じPCの `http://127.0.0.1:8000` とする。Codexまたは認可サーバーがHTTPのIssuerを受け付けない場合に限り、同じOAuthのまま、そのPC上のサーバーをHTTPSで中継して接続する。この中継は公開ホスティングではない。

ChatGPTからの利用、および不特定の利用者へ向けた公開はv0.4の対象としない。Accountaやデータベースを認証なしで外部公開しない。

## v0.5

v0.5では、v0.4までの機能に加えて、v0.3で登録した仕訳から作成する総勘定元帳を提供する。

- 仕訳用勘定科目ごとの仕訳明細を日付順に確認できる
- 対象期間、仕訳用勘定科目および部門で明細を絞り込める
- 各明細の借方金額、貸方金額および残高を確認できる
- 仕訳用勘定科目ごとの借方合計、貸方合計および残高を確認できる
- 総勘定元帳の明細から元の仕訳を確認できる
- 自分の組織の総勘定元帳だけを利用できる

総勘定元帳は仕訳をもとに作成し、同じ取引を独立したデータとして二重に登録しない。

通常のExcelエクスポートには、仕訳用勘定科目ごとの借方合計、貸方合計および残高をまとめた集計結果 `GeneralLedgerSummary` だけを追加する。

これとは別に、総勘定元帳の詳細Excelファイルを出力できる。詳細Excelでは仕訳用勘定科目ごとにシートを作成し、それぞれのシートへ対象期間の全明細を時系列で出力する。

## v0.6

v0.6では、v0.5までに登録した仕訳から、簡易的な損益計算書および貸借対照表を作成する。

- 仕訳用勘定科目にP/L・B/Sの表示区分と表示順を設定できる
- 対象期間を指定して損益計算書（P/L）を確認できる
- 基準日を指定して貸借対照表（B/S）を確認できる
- 収益、費用および当期損益を確認できる
- 資産、負債および純資産を確認できる
- 仕訳を追加・修正した結果をP/LとB/Sへ反映できる

P/LとB/Sは、登録されている仕訳をもとにその都度集計する。決算振替を行わなくても貸借を確認できるよう、P/Lで算出した当期損益をB/Sの純資産へ反映する。

通常のExcelエクスポートへ、損益計算書 `ProfitAndLoss` と貸借対照表 `BalanceSheet` を追加する。

v0.6では、残高試算表および厳密な開始・決算・繰越手続を提供しない。P/LとB/Sによって主要な集計結果を確認できればよいものとする。

## v0.7～v0.9

v0.7からv0.9では、v0.1からv0.6で提供した機能の連携、使いやすさおよび品質を改善する。

- 手入力実績と仕訳から集計する会計実績の扱いを整理する
- 固定資産、仕訳、総勘定元帳、P/LおよびB/Sの相互参照を改善する
- 検索、絞り込み、入力検証およびエラー表示を改善する
- 通常のExcelエクスポートと各機能の詳細Excelエクスポートを整える
- MCPから参照できる対象を、仕訳帳以外のマスタ、予算・実績、固定資産および財務諸表へ順に広げる
- MCP Toolの検索性、応答形式、認証、権限および監査性を改善する
- 組織ごとの権限とデータ分離を確認する
- 正式公開に必要な性能、セキュリティおよび運用上の問題を解消する

具体的な改善項目と各バージョンへの割り当ては、v0.6までの利用結果を踏まえて決定する。

## v0.10

v0.10では、v0.3で登録した証憑を会計処理へ活用するため、OCR、内容解析および重複判定を追加する。

- 証憑の登録後に非同期でOCRを実行し、文字列、ページおよび位置情報を抽出する
- OCR結果と必要に応じて証憑画像を解析し、発行元、取引日、支払期日、証憑番号、通貨、税抜金額、消費税額、税込金額および明細等を構造化する
- ファイルのハッシュによる完全一致と、発行元、証憑番号、取引日および金額等の組み合わせによる類似判定を行い、重複の可能性を利用者へ警告する
- OCRの生データ、構造化した解析結果、利用したプロバイダー、モデルまたはエンジンのバージョン、確信度および処理履歴を保存する
- OCR・文書解析の実装は特定サービスへ固定せず、ローカルで動作するオープンソースOCRと外部のDocument Intelligence系サービスを交換できる構成にする

## v0.11

v0.11では、v0.10の解析結果を利用して仕訳候補を生成し、一定の条件を満たす証憑から仕訳を自動作成できるようにする。

- 解析結果、仕訳用勘定科目マスタおよび過去の確定仕訳を利用し、借方・貸方の仕訳候補、根拠および確信度を生成する
- 定型ルールまたは所定の確信度を満たす仕訳候補から、未確定の仕訳を自動作成する
- 自動作成した仕訳は、利用者が内容を確認、修正および確定するまで帳簿へ反映しない
- 利用者による修正内容を保存し、以後の仕訳候補生成と定型ルールの改善に利用できるようにする
- OCR、内容解析、重複判定および仕訳生成の処理結果を追跡できる監査履歴を提供する

## v0.12

v0.12では、Accounta内の業務知識、操作説明、会計に関するメモおよびお知らせなどを蓄積・表示するため、管理アカウント向けの簡易CMS機能を追加する。

- 管理アカウントがMarkdown形式の文章を登録、編集、公開および非公開にできる
- タイトル、概要、本文、カテゴリ、公開状態、公開日時および更新日時を管理できる
- 公開済みの文章を専用画面で一覧表示し、個別ページではブログのように閲覧できる
- Markdownの見出し、箇条書き、表、リンク、コードブロックおよび引用を表示できる
- LaTeX等による数式を表示できるよう、数式記法を含むMarkdownの描画に対応する
- 未公開の文章は一般ユーザーに表示せず、管理アカウントだけが確認できる
- 文章の作成者、公開・非公開の変更履歴および更新履歴を管理できる

このCMSは、将来的に生成AIへ渡す業務知識および説明文の管理基盤としても利用する。生成AI連携では、利用者の権限に応じてAccountaの会計データ、管理コンテンツおよび関連する業務情報を検索・取得し、RAGに近い方式で要約、評価、説明および問い合わせへの回答を生成できるようにする。

ただし、v0.12では生成AIとの接続自体は必須とせず、Markdownと数式を正しく保存・表示できること、公開範囲を制御できることおよび将来の検索・埋め込み生成に利用できる構造でデータを保持することを要件とする。生成AIが作成した回答は、根拠となるデータや文章を参照できる形で表示し、会計上の確定判断や自動的な仕訳登録を無条件に行わない。

## v0.13

v0.13では、税理士試験の財務諸表論および日商簿記1級の会計学を対象とした理論問題の学習機能を追加する。

- 穴埋め問題、考え方・理由・比較を問う記述問題、定義・意義を問う記述問題を登録できる
- 問題、模範解答、解説、対象論点、難易度等を管理する
- 概念フレームワーク、退職給付会計、連結会計、研究開発費等の論点ごとに問題を分類する
- 選択した論点について、登録順またはランダムで問題を1問ずつ出題し、解答後に模範解答と解説を確認できる
- 初期問題は、会計基準等をMarkdown化した資料を基にCursorやCodex等で事前作成し、Seeder等を利用してDBへ登録する
- 以後は管理アカウントが管理画面から問題と解答を登録、編集、削除および公開・非公開にできる

Accounta自身が生成AI APIを利用して問題を生成する機能は、本バージョンでは対象としない。

## v0.14

v0.14では、会計基準、適用指針、実務対応報告、概念フレームワーク、企業会計審議会の公表資料等をAccounta上で閲覧できる会計ナレッジ機能を追加する。

- 会計基準等をMarkdown形式で管理し、ブラウザ上で読みやすく表示する
- 資料を論点や種類から選択して閲覧できる
- 見出し、目次、表等を整形し、PCだけでなくスマートフォンでも確認しやすい画面とする
- 資料名、発行主体、公開日、適用時期、参照元URL等を確認できる
- 会計基準等の改訂時には、新旧の資料を識別できるようにする

本機能は原本PDFの閲覧を目的とせず、会計基準等を必要なときに手軽に確認するための補助機能とする。

## v0.15

v0.15では、v0.13の理論問題を利用した本格的な反復学習機能を追加する。

- 利用者ごとに解答履歴、学習回数、正誤、自己評価および最終学習日時等を記録する
- 全体および論点ごとの学習進捗、定着度、復習対象および苦手分野を確認できる
- 未学習、誤答した問題、忘却が進んでいる問題等を優先して出題する
- 正答を繰り返した問題は出題間隔を延ばし、誤答した問題は短い間隔で再出題する
- FSRS等の間隔反復学習アルゴリズムを利用し、利用者ごとの記憶状態に応じて復習時期と出題順を決定する
- 学習アルゴリズムは将来変更できるよう、解答履歴と計算方式を分離して管理する

これにより、単なる理論問題集から、利用者ごとの記憶状態に応じて復習内容を最適化する会計理論学習サービスへ発展させる。
## v1.0

v1.0は、Accountaを一般の利用者へ正式に提供できる最初の公開リリースとする。

v1.0では、次のことが簡易的かつ安定してできることを完成条件とする。

- 予算・実績を管理できる
- 会社、組織、部門、予実管理科目および仕訳用勘定科目を管理できる
- 固定資産と手動で計算した減価償却費を管理できる
- 仕訳を手動で登録できる
- 仕訳から総勘定元帳を確認できる
- 仕訳からP/LとB/Sを確認できる
- 証憑を仕訳へ紐付けられる
- 証憑をOCRして主要項目を解析し、重複の可能性を確認できる
- 証憑の解析結果から仕訳候補と未確定仕訳を作成できる
- MCPを通じて許可された主要データを安全に参照できる
- 各種データをExcelへ出力できる
- 財務諸表論の理論問題を学習し、解答履歴、記憶の定着度および学習進捗を確認できる
- 基準通貨を設定し、外貨建ての取引および残高を管理できる
- 通貨ごとの金額、換算レート、換算日時および換算後の基準通貨金額を保持できる
- 外貨金額と基準通貨換算額を併記して確認できる

法定帳簿、税務計算および各種会計基準への完全準拠よりも、各機能が一貫して連携し、会計学習と基本的な管理業務に利用できることを優先する。

## v1.1

v1.1では、Apache Supersetを利用し、Accountaの会計・予実データをダッシュボードで表示できるようにする。

- 指標、表およびグラフで会計・予実データを確認できる
- 会計年度、年月、部門、予実管理科目および仕訳用勘定科目等で表示内容を絞り込める
- 予算、実績、差異および推移を確認できる
- 利用者は許可された会社・組織のデータだけを確認できる

Apache Supersetのすべての機能をAccounta内へ再実装することは目的とせず、Accountaのデータを分析するために必要なダッシュボード表示を提供する。

## v1.2以降

v1.2以降は、簡略化していた機能を段階的に高度化する。

- 開始記入、繰越試算表の作成および再振替仕訳による開始手続
- 決算整理前残高試算表の作成
- 決算整理仕訳
- 決算整理後残高試算表の作成
- 勘定の締切、決算振替および翌会計年度への残高繰越
- 決算整理後残高試算表からのP/L・B/S作成
- 仕訳の確定、締め済み期間の変更制限、取消・訂正仕訳および監査履歴
- 償却率、改定償却率、保証率等の固定資産項目の追加
- 減価償却費の自動計算と減価償却仕訳の自動生成
- 自動仕訳の対象拡大、高度な証憑処理および承認ワークフロー
- 銀行明細等の外部データ連携
- 複数通貨、為替差損益、期末換算および外貨建て債権・債務の評価

高度化する機能は必要性と優先度を確認しながら、個別のバージョンへ割り当てる。

外貨対応はv1.0以降に段階的に実装する。v1.0では基本的な通貨・換算レート管理と基準通貨での表示を優先し、期末換算、為替差損益の自動計算、複数の評価方法および外貨決済の高度な処理はv1.2以降で扱う。

## v1.0以降：モバイルUI強化

現行のUIはモバイル端末での閲覧性および操作性が十分ではなく、特に仕訳、証憑、固定資産、予算・実績などのレコード一覧が見づらい。そのため、v1.0以降の計画として、モバイル端末に特化した表示および操作体験を段階的に整備する。

- スマートフォンおよびタブレットの画面サイズに適した専用レイアウトを提供する
- レコード一覧を、横幅の広い表形式に依存しないカード形式・要約形式などで表示できるようにする
- モバイル画面で重要な項目を優先表示し、詳細情報は展開表示または詳細画面で確認できるようにする
- 一覧の検索、絞り込み、並び替えおよびページ移動をモバイル操作に適したUIで提供する
- 登録・編集画面の入力項目、ボタンおよび確認操作をタッチ操作に適した大きさと配置にする
- 横スクロールを必要最小限にし、主要な業務操作を縦方向の画面遷移で完結できるようにする
- モバイルでの表示速度、読み込み中の状態および通信エラー時の操作性を改善する

モバイルUI強化は、現時点では実装対象とせず、v1.0以降の優先度および利用状況を確認しながら段階的に着手する。まずはレコード一覧の見やすさを優先し、その後に登録・編集、ダッシュボードおよび証憑確認の操作体験を改善する。

## Accountaは何者か

Accountaは、個人事業主や中小企業を主な対象とし、日々の取引、証憑、予算・実績、固定資産および会計データを一元管理するための、シンプルな管理会計・業務支援アプリケーションである。単なる仕訳入力ソフトやERPの代替ではなく、利用者が自社の業務データを理解し、管理・分析し、将来的には生成AIと対話的に活用するための基盤を目指す。

Accountaの役割は、会計データを正しく記録・集計することに加え、証憑、業務知識、管理コンテンツおよび分析結果を同じ文脈で扱えるようにすることである。将来的には、Accounta自身のデータと外部ERPのデータを権限管理のもとでつなぎ、管理部門向けの業務ロジック、分析およびAI活用のレイヤーとして機能する。

## 将来展望：SAP S/4HANAおよびAIとの連携

Accountaは、将来的に個人のExcelに分散した管理データだけでなく、全社ERPに蓄積されたデータと、管理部門の業務知識・分析手順を組み合わせて利用できる基盤へ発展させる。

### SAP ERPとSAP S/4HANAの位置付け

SAP ERP（ECCなど）とSAP S/4HANAは、どちらも会社全体の会計、購買、販売、在庫、固定資産などを統合管理するERPである。S/4HANAはSAP ERPの後継世代にあたり、基本的にSAP HANAデータベースを基盤として利用する。

したがって、利用者のPCにインストールされているSAP GUIは、ERPへ接続するための操作クライアントであり、ERP本体やデータベースそのものではない。将来のAccounta連携先として想定するのは、HANAデータベースを直接参照することよりも、SAP S/4HANAが提供する業務データおよびAPIである。

### 想定する連携構成

将来構想では、次のような構成を想定する。

```text
ChatGPT
   │ MCP
   ▼
Accounta ── OData / REST / RFC等 ── SAP S/4HANA
   │                                      │
   │                                      ▼
   │                                 SAP HANA
   ▼
PostgreSQL
```

Accountaは、予算・実績、経費分析、固定資産、資金管理などの管理業務ロジックと、Accounta自身が保持する補助情報を担当する。S/4HANAからは、会計伝票、売上、購買、固定資産、原価、在庫およびマスタなど、全社ERPの正式な業務データを取得する。

HANAデータベースをAccountaから直接読むのではなく、S/4HANAが提供するAPI等を介して、認証、権限、業務上の意味およびデータ取得範囲を管理する。これにより、SAP内部のテーブル構造に利用者やAccountaの業務ロジックが過度に依存することを避ける。

### MCPを利用したAI連携

将来的には、SAP Integration SuiteのMCP Gateway等を利用して、S/4HANAのAPIまたはRFCベースのバックエンドをMCP経由で利用できる可能性がある。また、AccountaがS/4HANAへ接続するMCP Clientを持つ構成も検討対象とする。

ただし、S/4HANA本体に常にMCP Serverが標準搭載されていると決めつけてはならない。実際の利用可否は、対象環境でのSAP Integration Suiteの契約・Service Plan、公開されているAPIまたはRFC、認証方式、権限設定および社内ネットワーク要件を確認したうえで判断する。SAP GUIがPCにインストールされていることだけでは、MCP連携の可否は判断できない。

連携方式は、処理内容に応じて次のように使い分ける。

- 定型的なデータ取得や業務処理：AccountaからS/4HANAのOData、RESTまたはRFC等を利用する
- AIが必要なデータや処理手順を選択する場合：S/4HANA側のMCP GatewayまたはMCP Serverの利用を検討する
- Accountaの管理データとS/4HANAの実績を組み合わせる場合：Accountaの業務ロジックを経由して統合・分析する

### 目指す利用イメージ

利用者は、ChatGPTからAccountaおよびSAPに対して自然言語で依頼する。例えば、Accountaから予算差異を取得し、S/4HANAから該当部門の会計実績や原価情報を取得し、両者を組み合わせて差異の原因を分析する、といった処理を目指す。

```text
利用者
  │ 「8月の予算超過部門について、S/4の実績も確認して原因を分析して」
  ▼
ChatGPT
  ├─ Accounta MCP：予算差異・管理データを取得
  ├─ SAP MCPまたはAccounta経由のAPI：会計・原価実績を取得
  └─ 差異、推移および原因を分析して説明
```

この構成におけるAccountaは、SAP S/4HANAを置き換えるERPではない。S/4HANAの全社データとAccountaの管理データ・分析知識をつなぎ、管理部門向けの業務ロジック、分析およびAI利用のレイヤーとして機能する。

なお、SAP S/4HANA連携およびSAP側のMCP利用は、現時点で確定した実装仕様ではなく将来構想である。実装を開始する前に、対象組織のS/4HANA構成、利用モジュール、API公開状況、Integration Suiteの利用状況、接続方式、認証および権限要件を確認する。
