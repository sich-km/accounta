# Accounta v0.1 実装指示書

この文書は、[Accounta要求仕様書](Accounta_SPEC.md)のv0.1を実装・検証するための制御上の基準を定義する。

特定のAI製品や実装担当者に依存しない。細かな実装方法は、既存コードの規約と自動テストを優先する。

---

# 1. 実装範囲

v0.1で実装する機能:

- ユーザーIDによる認証とプロフィール
- CompanyとOrganizationによる所属管理
- システム管理者によるCompany管理
- 部門マスタ
- 勘定科目マスタ
- 予算・実績明細
- レコード件数を表示する簡易ダッシュボード
- Excelエクスポート
- 初期データSeeder

仕訳、帳簿、財務諸表、データImportおよびBIダッシュボードは実装しない。

---

# 2. 技術基準

- Laravel 13
- PHP 8.3以上
- Blade
- Tailwind CSS
- Jetstream Livewire Stack
- MySQL 8.4 LTSを正式な完了判定DBとする
- UI Localeは日本語、Timezoneは `Asia/Tokyo` とする

SPA、公開APIおよび不要な独自JavaScriptは追加しない。

実装はLaravelの標準機能と既存プロジェクトの構成を優先する。要求にない依存パッケージを追加しない。

---

# 3. 認証制御

- 認証識別子にはメールアドレスではなくユーザーIDを使用する
- ユーザーIDは正規化し、大文字小文字を区別せず一意にする
- 登録時にメールアドレスを要求しない
- ユーザーとOrganizationは同一Transactionで作成する
- プロフィール更新でユーザーIDや所属を変更させない
- 無効化した認証機能の画面とRouteを公開しない

無効化対象:

- メール認証
- パスワードリセット
- 二要素認証
- Passkeys
- API Token
- アカウント削除
- Jetstream Teams

---

# 4. 所属・権限制御

Companyを最上位、Organizationを業務データの分離単位とする。

- UserはCompanyとOrganizationの両方に所属する
- Organizationは必ずCompanyに所属する
- Department、Account、MonthlyAmountはOrganizationに所属する
- UserのCompanyとOrganizationの組み合わせに矛盾を作らない
- 業務画面の取得・更新・削除はログインユーザーのOrganizationで制限する
- 他Organizationのリソースを指定した場合は、その存在を不用意に開示しない

ユーザー種別:

- `admin`: Company管理が可能なシステム管理者
- `company_admin`: 所属Organizationの部門マスタと勘定科目マスタを管理できる会社管理者
- `user`: 部門マスタと勘定科目マスタを参照できる一般ユーザー

Companyにはシステム用の特別種別を設けない。Company情報の更新では企業コード、企業名および会計年度開始月を変更可能とする。

---

# 5. マスタ制御

DepartmentとAccountに共通する制御:

- コードはOrganization内で一意にする
- コードと名称は保存前に正規化する
- 削除機能は提供せず、有効・無効を切り替える
- `admin` と `company_admin` は一覧表示、登録、編集および有効・無効切替ができる
- `user` は一覧表示だけができ、登録、編集および有効・無効切替はできない
- `user` の画面には登録、編集および有効・無効切替の操作を表示しない
- 無効なマスタは新規の予算・実績明細で選択できない
- 既存明細が参照している無効なマスタは維持できる
- 他Organizationのマスタを参照・変更できない

Accountは資産、負債、純資産、収益、費用の区分を保持する。

---

# 6. 予算・実績制御

MonthlyAmountは集計結果ではなく独立した明細として保存する。

- 予算と実績は区分によって識別する
- 同一の年月、部門、勘定科目および区分に複数明細を許可する
- 予算と実績を直接関連付けない
- 対象年月はDB上で月初日として保持する
- 金額は小数2桁までとし、0、正数、負数を許可する
- 登録元は画面入力させず、サーバー側で手入力として設定する
- DepartmentとAccountはMonthlyAmountと同じOrganizationに限定する
- 一覧は対象年月の降順を基本とし、ページネーションする
- 削除は選択した明細だけを対象とする

v0.1では実績も手入力する。仕訳由来の実績はv0.2で再設計する。

---

# 7. ダッシュボード・プロフィール

ダッシュボードでは、ログインユーザーのOrganizationを基準に次を取得する:

- 予算明細数
- 実績明細数
- 有効な部門数
- 有効な勘定科目数

高度な集計処理やグラフは追加しない。

プロフィールでは現在のCompany、Organizationおよびユーザー種別を表示する。所属情報をプロフィールから変更させない。

---

# 8. Excelエクスポート制御

- 認証済みユーザーだけが実行できる
- ログインユーザーのOrganizationだけを出力する
- `admin` は予算・実績、部門、勘定科目およびCompany・Organization情報を1つのWorkbookへ出力する
- `company_admin` と `user` は予算・実績、部門および勘定科目を1つのWorkbookへ出力し、Company・Organization情報は出力しない
- 各データを別Sheetに分ける
- Sheet名は `予算・実績`、`部門マスタ`、`勘定科目マスタ` および `会社・組織情報` とする
- 各Sheetの列幅は、見出しと通常のデータがExcelで読みやすく表示される幅に調整する
- 無効なマスタも出力する
- 文字列を意図せずExcel数式として評価させない
- 同期Downloadとし、出力履歴をサーバーへ保存しない
- Import機能は作らない

---

# 9. 初期データ

Seederは次を再現可能に作成する:

- 企業コード `km` のCompany
- 初期Organization
- `admin` ユーザー
- `company_admin` ユーザー
- `user1` ユーザー
- 標準部門
- 標準勘定科目

Seederは再実行可能とし、不要な重複を作らない。初期パスワードはランダムに生成し、ソースコードへ固定値を保存しない。

---

# 10. データ整合性・セキュリティ

- 所属関係はApplicationのValidationだけでなくDB制約でも保護する
- 外部キーで参照中の主要データを不用意に削除できないようにする
- リクエストから所属Organizationやサーバー管理項目を自由に設定させない
- 認証、権限、入力Validation、Organization分離をサーバー側で実施する
- Excelへ出力する文字列を安全に扱う

---

# 11. 自動テスト

少なくとも次をFeature Testで確認する:

- ユーザーIDによる登録・ログインと重複拒否
- 無効化した認証機能を利用できないこと
- 登録失敗時に不完全なUserやOrganizationが残らないこと
- DepartmentとAccountの登録、編集、有効・無効切替
- DepartmentとAccountに対するユーザー種別ごとの表示・変更権限
- マスタコードの重複拒否と正規化
- 予算と実績の登録、一覧、編集、削除
- 同一条件の複数明細
- 年月、金額およびマスタ選択の主要な入力エラー
- 他Organizationのデータを参照・変更できないこと
- Company管理の権限
- Seederの再実行性
- Excelの日本語Sheet名、列幅、ユーザー種別ごとの出力内容、Organization分離および文字列の安全性

---

# 12. v0.1 完了条件

次をすべて満たした場合にv0.1完了とする:

- 要求仕様書のv0.1機能を操作できる
- 対象外機能が意図せず有効になっていない
- 全Feature Testが成功する
- MySQL 8.4で全MigrationとFeature Testが成功する
- フロントエンドの本番Buildが成功する
- 重大な既知不具合がない

完了判定は、機能が存在することだけでなく、権限、Organization分離、入力失敗および正式DBでの検証結果を含めて行う。

---

# 13. 実装時の運用

- 要求を満たす最小限の変更を行う
- 既存のコード規約とテスト構成に合わせる
- 変更した振る舞いには対応するテストを追加する
- 将来バージョンの機能を推測して先行実装しない
- 要求が曖昧でデータ構造や権限に影響する場合は、実装前に確認する
- 内部実装だけの変更では要求仕様書を更新しない
